/**
 * Local branding config. Used by the app so it never depends on DB branding.
 * Edit this file to change app name, logo, favicon, and base URL.
 */
export const LOCAL_BRANDING = {
  appName: "ModelClone",
  logoUrl: "/logo-512.png",
  faviconUrl: "/logo-512.png",
  baseUrl: "https://modelclone.app",
};
