import { getDevicePickerOptions } from "../../../src/repurposer/data/device-profiles.js";

/** Full device list for repurposer (Apple detailed profiles + catalog). */
export const REPURPOSE_DEVICE_OPTIONS = getDevicePickerOptions();
