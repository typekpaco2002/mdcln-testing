node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.env.*
!.env.example
tmp/
tmp-index.html
/public
# FFmpeg binaries copied at Vercel build (scripts/ensure-ffmpeg.js)
bin/
# Default: ignore local uploads; commit the canonical NSFW Comfy graph for RunPod.
attached_assets/*
!attached_assets/nsfw_core_workflow.json
!attached_assets/nsfw_img2img_v2promax_workflow.json

# Nested repo checkout — keep files, not embedded .git
runpod-mdcln/.git/

# Satellite Vercel wrapper: local submodule checkout only (see modelclone-api/README.md)
modelclone-api/core/

# Python reelscraper local venv
reelscraper/.venv/

__pycache__/
*.pyc

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Figma push pipeline — ephemeral capture IDs + auth state
/scripts/figma-captures.json
/scripts/figma-captures-result.json
/scripts/figma-auth-state.json
